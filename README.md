# fastapi_model_disponibility
API to get result of model



```
cd project
uvicorn project.main:app --reload
```

To check log
```
uvicorn main:app --host 0.0.0.0 --port 8000 --reload --log-level info
```

```
docker-compose -f docker-compose.dev.yml up
```